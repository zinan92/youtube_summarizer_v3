# YouTube Data API v3 Configuration (optional - for enhanced metadata)
# Get your API key from: https://console.developers.google.com/
# 1. Go to Google Cloud Console
# 2. Create a new project or select existing
# 3. Enable YouTube Data API v3
# 4. Create credentials (API Key)
# 5. Copy the API key below
YOUTUBE_API_KEY=your_youtube_api_key_here

# OpenAI API Configuration (required for AI processing)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model Selection
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Chunking Configuration for Large Transcripts
# CHUNKING_THRESHOLD: When to start chunking (default: 20000)
CHUNKING_THRESHOLD=20000

# CHUNK_SIZE: Characters per chunk (default: 35000)
CHUNK_SIZE=35000

# CHUNK_OVERLAP: Overlap between chunks for context (default: 500)
CHUNK_OVERLAP=500

# Audio Chunking Configuration (NEW - Solves timeout issues!)
# AUDIO_CHUNK_DURATION: Duration of each audio chunk in seconds for large files (default: 180 = 3 minutes)
AUDIO_CHUNK_DURATION=180

# MIN_FILE_SIZE_FOR_CHUNKING: Minimum file size in bytes to trigger audio chunking (default: 10485760 = 10MB)
MIN_FILE_SIZE_FOR_CHUNKING=10485760

# Concurrency and Timeout Configuration
# MAX_CONCURRENT_CHUNKS: Maximum concurrent API calls for chunk processing (default: 3)
MAX_CONCURRENT_CHUNKS=3

# API_TIMEOUT: Timeout for OpenAI API calls in seconds (default: 120)
API_TIMEOUT=120

# DOWNLOAD_TIMEOUT: Timeout for YouTube downloads in seconds (default: 300)
DOWNLOAD_TIMEOUT=300

# TRANSCRIPTION_TIMEOUT: Timeout for Whisper transcription in seconds (default: 1800)
TRANSCRIPTION_TIMEOUT=1800

# Retry Configuration
# MAX_RETRIES: Maximum number of retries for failed operations (default: 3)
MAX_RETRIES=3

# RETRY_DELAY: Initial delay between retries in seconds (default: 1)
RETRY_DELAY=1